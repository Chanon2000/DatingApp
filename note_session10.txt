[Introduction]
    1. Angular Template forms
    2. The CanDeactivate Route Guard
    3. The @ViewChild decorator => เอาไว้ใช้เข้าถึง template จากที่ component
    4. Persisting changes to the API
    5. Adding loading indicators to the client app
    6. Caching data in Angular services

[Creating a member edit component]
    ng g c member-edit --skip-tests


[Adding a Can Deactivate route guard]
    ng g guard prevent-unsaved-changes --skip-tests
    เลือก CanDeactivate

[Adding loading indicators]
npm install @angular/cdk (ถ้า install ngx-spinner แล้วดัน error)

ng add ngx-spinner
    - ใส่ add แบบนี้ของ angular มันจะจัดการ configure ต่างๆให้เราได้ง่ายขึ้น

ng g s busy --skip-tests

ng g interceptor loading --skip-tests